# core

## Role

Central orchestration and game state layer. Controls all game flow, manages persistent and battle state, and produces render-ready snapshots for scenes.

## Responsibilities

- Own the phase state machine — validate and execute all game transitions
- Own separate runtime references for persistent campaign data, debug session data, and battle state
- Initialize battle state from the active player session (campaign or debug)
- Build display snapshots (bench cards, stats, upgrades) for each phase
- Invoke progression resolvers from `src/progression/` to build battle-ready unit input
- Isolate debug session state from campaign — same `RosterState`/`InventoryState` types, never shared instances

## Key Files

- `PhaseManager.ts` — the transition-pipeline **coordinator**; single entry point for all transitions via `transition(action): PhaseTransitionResult`. The return value is call-scoped: it reports acceptance and the transient `battleFeedback` produced by _this_ invocation, and is the only channel for battle feedback (see `phaseEffectsResult.ts` on the removed stored-result protocol). It sequences **injected** collaborators and implements none of their rules — routing, metadata derivation, **all mutation and lifecycle work**, snapshot rebuilding, scene control and mutation notification all live in the modules below. `PhaseManagerClass` takes a **required** `PhaseManagerDependencies` object (`deriveMetadata`, `resolveTransition`, `applyEffects`, `rebuildSnapshot`, `notifyPhaseChanged`): no field is optional and the class holds no defaults, so a manager cannot exist unless someone composed it explicitly and a test can never fall through to a real owner that mutates the `GameState` singleton. `PhaseSceneSynchronizer` is deliberately **not** in that set — it arrives later, through `init()`, because scene control only exists once Phaser has booted. `createProductionPhaseManager()` (Stage 3D) is the **only** production composition path: it resolves the five real collaborators, and both the exported `PhaseManager` singleton and `tests/core/helpers/phaseManagerLifecycleHarness.ts` are built through it, so the characterization suite exercises production wiring. It calls `createDefaultPhaseActionEffects()` **inside its own body**, never at module scope — the controller owns a gameplay RNG stream pair, so a hoisted one would let a lifecycle reset in either manager silently reseed the other; that guarantee is pinned by `tests/core/phaseManagerProductionComposition.test.ts`, because hoisting compiles, keeps navigation working and still passes the RNG characterization suite. An effects or snapshot throw stops the pipeline and leaves the phase uncommitted — it does **not** roll back mutations effects already made; the pipeline is deliberately non-transactional. Its entire public surface is `init()`, `getPhase()` and `transition()`; it holds no RNG, no domain state, and inspects no action or phase discriminator. Both source rules are enforced by `evaluatePhaseManagerPolicy()` in `scripts/phase-manager-policy.mjs` — one call site in `check-boundaries.mjs` combining the discriminator scan with a public-API AST allowlist (`PHASE_MANAGER_PUBLIC_API`) that fails closed on index signatures, computed and unnamed members, and public/`readonly` constructor **parameter properties** while permitting the constructor itself and plain parameters — plus a nine-entry import allowlist, all three pinned in `tests/scripts/`. Collaborator ordering, exact argument identity and identity-based classification are pinned by `tests/core/phaseManagerOrchestration.test.ts`. Gone rather than rehomed: `applyActionSideEffects()`, `resetGameplayRngStreams()`, the generic battle-runtime teardown block, `getDebugState()`, `setRngStreamsForTest()`, `resetRngStreams()` (all three had zero callers — read `GameState` directly, and inject RNG through the `core/random` mock seam), `rebuildSnapshot()`, `wouldClearMap()`, and `refreshSnapshot()` (which committed `this.phase` while bypassing classification, effects and scene sync). The one thing it still owns outright is the **navigation/mutation classification**, computed by reference identity (`resolvedPhase === previousPhase`) as a local variable: it is deliberately not exported, because a scene that could read it would be making routing decisions that belong to `resolveTransition`. Identity, never discriminators — a mutation-only rebuild usually returns a _new_ object (so classifying after the rebuild would misreport every in-battle mutation) and `replay` resolves to a _copy_ of the battle phase (so comparing `phase.type` would misclassify it as a mutation)
- `phaseActionEffects.ts` (Stage 3C) — the write side's single entry point: `createDefaultPhaseActionEffects(): PhaseActionEffectsController`, whose `apply(action, previousPhase, resolvedPhase)` runs the action's effect and then the generic battle-runtime finalizer, returning a call-scoped `PhaseEffectsResult`. It is a **dispatcher and stateful composition boundary, not a domain owner**: one exhaustive `switch` over `PhaseAction` ending in a `never` assertion, plus the cross-domain ordering each lifecycle action requires. It owns the per-manager gameplay RNG pair in a factory closure — never at module scope, or two `PhaseManagerClass` instances would share random state — and every lifecycle action (`new_game`, `init_debug`, `reset_debug_session`, `exit_to_menu`) replaces the **entire** pair rather than reassigning a field. `createPhaseActionEffects(dependencies)` is the injectable form; `PhaseActionEffectsDependencies` has **no optional fields and no production fallbacks**, so a test that forgets a fake gets a compile error instead of a silent real-owner call that mutates the `GameState` singleton. It imports owners only: no `GameState`, no `PlayerSessionStore`, no `data/**` catalog, no snapshot builder, no Phaser, and neither half of the read side. Since Stage 4A that is enforced rather than documented: its complete import set is registered edge-by-edge in `ORCHESTRATION_COLLABORATOR_REGISTRY` as an `effects` facade, whose permitted roles are only `neutral-contract`, `effects-owner` and `effects-infrastructure` — a state reader or a snapshot projection cannot be added without changing the facade's _kind_. It is the designated place to inspect action and phase discriminators
- `campaignLifecycle.ts` (Stage 3C) — `initializeNewCampaign()`: the campaign-side sibling of `debugLifecycle.ts`, owning campaign _container_ creation. Exists so the five production data catalogs stay out of `phaseActionEffects`, which decides when a campaign is created but must not know what one is made of. Always creates (no idempotent guard — "New Game" discards any run in progress) and touches nothing else: clearing debug state, disposing the battle runtime and resetting RNG are cross-domain sequencing owned by the effects facade. Since Stage 4C its complete dependency set is an exact, reviewed policy in `ORCHESTRATION_COLLABORATOR_IMPORT_POLICIES`, checked against the real source in both directions — it cannot absorb debug-session construction, and a removed catalogue cannot leave a dormant permission behind
- `campaignWorldTransitions.ts` (Stage 3C) — pure campaign-world **write** transformations, the counterpart to the read-only `worldMapProjection.ts`: `applyMovePartyToCampaign(campaign, partyPos)` and `applyEncounterDefeatedToCampaign(campaign, mapId, triggerPos)`. Every function is `(CampaignState, ...) => CampaignState` — total, immutable at every level (entity state → `entityStates` → `SubMapState` → `subMapStates` → `WorldState` → `CampaignState`), and storage-free; installing the result is the caller's job. A missing sub-map is a no-op returning the campaign by identity. The `${x},${y}` entity key is the shared key space `world/mapCompletion.ts` and `world/mapLogic.ts` read, not local convention. Purity is pinned by a `PURE_CORE_FILES` entry. Read and write live in separate modules on purpose: one combined "world" module would be importable by the read pipeline for its mutations and by the write pipeline for its projections
- `battlePhaseEffects.ts` (Stage 3C) — the application-level owner of battle runtime reads and writes: `startBattleRuntime()`, `replayBattleRuntime()`, `applyBattleRuntimeMutation()` (the closed `BattleRuntimeMutationAction` union, dispatched to lifecycle/control/preview/turn/placement and ending in a `never` assertion), `finalizeBattleSessionOnExit()`, `clearBattleRuntimeIfPresent()` and `teardownBattleRuntimeAfterTransition()`. Pure battle rules are **not** here — they stay in `phaseHandlers/battlePhaseHandler`, `battleStart` and `battle/`. Every operation resolves the runtime through `requireBattleRuntimeForPhase()` **in its own body**, so none depends on another having validated first. Only `BattleActionFeedback` leaves the module; the handler result, runtime, `BattleState`, `TurnContext`, pending intention and unprojected directive are excluded by `PhaseEffectsResult`'s type. Generic teardown is structural — `previous phase is battle` + `resolved phase is not` — never an action-name catalogue, which is what keeps future battle-to-non-battle transitions covered. As the one production holder of the write capability its public surface is pinned too — those six functions plus the `BattleRuntimeMutationAction` type, in `RUNTIME_OWNERSHIP_EXPORT_POLICIES`; adding an export here, or re-exporting anything from `battleRuntimeWriteAccess`, fails `check-boundaries.mjs`. Stage 4C adds the inbound half: its complete dependency set is an exact, reviewed policy in `ORCHESTRATION_COLLABORATOR_IMPORT_POLICIES`, so the write owner can never acquire a snapshot builder or a metadata module — producing render state stays the read side's job
- `phaseHandlers/worldPhaseHandler.ts` (Stage 3C) — the storage seam for world mutations: `applyMovePartyPhaseAction(action)` and `applyBattleWorldConsequence({ previousPhase, outcome })`. Same shape as its three sibling handlers — resolve storage, delegate the rule to `campaignWorldTransitions.ts`, write once — and it owns no rules. The world consequence is the only `sessionSource`-keyed branch left in teardown: victory only, campaign only. It resolves and validates the runtime **itself** and branches on `runtime.sessionSource`, deliberately repeating the lookup `finalizeBattleSessionOnExit` just performed, so the owner stays correct when invoked independently and a mismatch throws before any campaign write. Because the runtime is resolved before the outcome guard, a corrupted runtime throws even on a defeat exit. `mapCleared` is not derived here — that is `phaseTransitionMetadata`'s job and it runs before any effect
- `phaseTransitionMetadata.ts` (Stage 3B) — `derivePhaseTransitionMetadata(currentPhase, action): PhaseTransitionMetadata`: the one owner of the stateful lookups the pure resolver must not perform. Currently computes `{ mapCleared }` for a campaign battle victory by reading `MAP_DEFINITIONS` and the campaign sub-map state and delegating the rule to `world/mapCompletion.ts`. Every irrelevant or incomplete case (non-`exit_battle`, defeat, non-battle phase, debug session, missing `mapId`/`triggerPos`, unknown map, missing map state) returns the shared frozen `{ mapCleared: false }` without touching campaign storage — a debug battle never falls back to campaign state. Read-only: no mutation, no Phaser, no scene/effects/snapshot/lifecycle imports — registered as a `metadata` facade in `ORCHESTRATION_COLLABORATOR_REGISTRY`, so an effects owner or snapshot projection is rejected by role, and `phaseTransitionResolver` is deliberately absent from its collaborators
- `battleRuntimeStorage.ts` (Stage 4B) — the single nullable cell holding the active battle attempt, and **nothing else**: no phase check, no `sessionSource` validation, no lifecycle rule, no field-specific setter. Exactly three exports (`readBattleRuntimeSlot`, `writeBattleRuntimeSlot`, `clearBattleRuntimeSlot`) — not a description of the current file but an enforced surface, pinned in `RUNTIME_OWNERSHIP_EXPORT_POLICIES` and checked against the real source by `scripts/module-export-policy.mjs`; installation preserves the supplied object's identity, a second installation replaces the complete reference, and clearing is idempotent. In **production** it is importable only by the two gateways below — enforced by `RESTRICTED_IMPORT_TARGETS` in `scripts/orchestration-boundary-rules.mjs`, which restricts the _importer_ direction rather than the dependency direction, because an outbound allowlist cannot express "who may pick up this authority". Tests may import it directly, and only for fixture setup, cleanup, observation and deliberately corrupted fixtures — constructing a source-mismatched runtime is impossible through any production path, which is exactly why the corruption characterization tests need the raw primitive
- `battleRuntimeAccess.ts` (Stage 3B; re-seated on storage in 4B) — `requireBattleRuntimeForPhase(phase)`: the single seam resolving the active `BattleRuntimeContext` for a battle phase, throwing `'Battle runtime is not initialized'` on an empty slot and asserting `runtime.sessionSource === phase.sessionSource`. Since Stage 4B it is the **only** production read path — the unvalidated `GameState.getBattleRuntime()` that used to sit beside it is gone, so no caller can skip the session-isolation guard. The initialization error moved here from `GameState` deliberately: storage returning `null` is a legitimate state (no battle active), not an error, so the throw belongs to the gateway that was asked for a runtime. Read-only by responsibility — it exposes this lookup and nothing else; installation and teardown are the write gateway's job. `requireBattleRuntimeForPhase` is its **entire** public surface, pinned in `RUNTIME_OWNERSHIP_EXPORT_POLICIES`. That is what makes the point above enforceable rather than conventional: this module may legitimately import storage, so without a closed export surface a single `export { writeBattleRuntimeSlot } from './battleRuntimeStorage'` — or the same forwarded under the name `requireBattleRuntimeForPhase` — would hand the write capability to every importer of the read gateway with the import checker still green
- `battleRuntimeWriteAccess.ts` (Stage 4B) — the battle-runtime **write capability**: `installBattleRuntime(next)` and the idempotent `clearBattleRuntime()`. Holding an import of this module _is_ the authority to replace or dispose the active battle attempt, so exactly one production module may import it — `battlePhaseEffects`, the lifecycle owner. Complete replacement only: there is deliberately no partial setter, so no action can leave the runtime half-updated between two writes. Performs no read, no validation, no phase inspection and no battle calculation. It is a separate module from `battleRuntimeAccess` for exactly one reason — the two have different _importer_ policies, and merging them would make that distinction unexpressible. Its public surface is exactly those two functions, pinned in `RUNTIME_OWNERSHIP_EXPORT_POLICIES`, so the capability cannot be re-exported onward by its one permitted importer either
- `itemInteractionStorage.ts` — the item-interaction cells, and **nothing else**: no phase check, no ownership rule, no lifecycle. The state is `closed | choosing_action | confirming_use` (`ItemInteraction`, declared in `shared/snapshotTypes.ts` so a projection can NAME it without gaining the ability to write the cell), where `closed` is the `null` slot — which is what makes "at most one modal is open" a storage property rather than a UI rule. Exactly four exports (the `ItemInteraction` re-export aside: `readItemInteractionSlot`, `writeItemInteractionSlot`, `clearItemInteractionSlot`), pinned in `RUNTIME_OWNERSHIP_EXPORT_POLICIES`, and importable in production only by the two gateways below. Unlike the battle runtime it is **keyed by `PlayerSessionSource`**, not a single cell: `BattleRuntimeContext` carries its own `sessionSource` and `requireBattleRuntimeForPhase` asserts it, and the same guarantee is needed here because `instanceId`/`unitTemplateId` prove nothing about ownership — a debug reset recreates the _same_ authored instance ids. Reads return a defensive copy, so a reader cannot reassign a stored request and bypass the write gateway. Two managers on the SAME session still share a slot, exactly as they already share `GameState`
- `itemInteractionAccess.ts` — the interaction **read** gateway: `readItemInteractionForPhase(phase)`, its entire pinned surface. Returns a request only when the phase can account for it — an equip screen, whose own session holds it, still showing the character it targets. That is the ownership assertion `requireBattleRuntimeForPhase` performs, expressed as a filter rather than a throw because an unjustified request is a normal state (the screen moved on), not corruption. It is deliberately **not** how disposal finds a stale request: it returns null in exactly the cases teardown exists to clean up
- `itemInteractionWriteAccess.ts` — the interaction **write capability** (`setItemInteraction`, `clearItemInteraction`). Holding an import of this module _is_ the authority to open, advance or dispose an item interaction, so exactly one production module may import it — `phaseHandlers/itemUsePhaseHandler`. A separate module from the read gateway for the same reason the battle-runtime pair is split: different _importer_ policies, which one merged module could not express
- `itemUsability.ts` — the out-of-combat item **read model**: `evaluateItemUsability` (plus the internal `evaluateItemUse` the executor shares), for backpack `consumable` AND `usable` items alike. It owns the whole eligibility decision — item resolution, effect support, amount validity, target life state, max HP **from the supplied catalog**, and the arithmetic finiteness pre-checks. A separate module from the executor so `equipmentScreenSnapshot` can be allowed this one and not `itemUse`: "does not call" and "cannot reach" are different guarantees, and only the second is enforceable
- `itemUse.ts` — the out-of-combat item **executor**: `useItem` returns the complete next `{ roster, inventory }` or a typed failure, and installs nothing. It adds no rule of its own — every check comes from `itemUsability` — and both delegates (`applyPermanentStatBoostToRoster`, `consumeBackpackItem`) receive pre-validated data and mutate nothing, so a partial apply is impossible
- `phaseHandlers/itemUsePhaseHandler.ts` — the item-use domain owner and the ONLY holder of the interaction write capability: `openItemActions`, `beginItemUseConfirmation`, `applyItemActionSelection`, `clearItemInteractionIfPresent`, `teardownItemInteractionAfterTransition` and `applyItemUsePhaseAction`. `applyItemActionSelection` owns stale-interaction disposal, in a load-bearing order: **match first** (a stored `choosing_action` for this screen's selected character AND this action's instance — a mismatched action must never dispose someone else's interaction), then re-validate the matching request live against the current session, then **clear** on failure with no roster or inventory write, or clear and advance on success. Hiding a modal in the projection would not settle this: the stored interaction is authoritative and, left in place, would block reopening or reappear later. Note the scope — this runs on a SELECTION; merely losing Use eligibility while the window is open does not close it, because Equip may still be perfectly usable. Its export surface is pinned for the same reason `battlePhaseEffects`'s is — without it, one re-export would hand the capability to every importer with the import checker still green. Teardown takes **both** phases, like `teardownBattleRuntimeAfterTransition`: the owner comes from the PREVIOUS phase because the resolved one often cannot supply it (`main_menu`, `debug_level_select` and `world_map` carry no `sessionSource`), and the disposal is an unconditional idempotent clear of that owner's slot rather than a filtered read first
- `battleItemSettlement.ts` — the one conversion from an attempt's `consumedItems` to the next persistent inventory: `settleBattleItemConsumption({ inventory, catalog, records })`. Pure — it returns the next inventory and installs nothing. Every record is validated before anything is written (duplicate ids, a definition that no longer matches the instance, and — through `inventory.removeItemInstances` — placement, ownership and metadata compatibility), so a corrupted attempt fails loudly with the session untouched rather than half-settled. Records carry no slot: `equipped_usable` re-derives it through inventory validation rather than trusting a value carried across the attempt boundary. An empty record list returns the input inventory BY IDENTITY, so an ordinary battle exit publishes no new object
- `phaseSnapshotRebuilder.ts` (Stage 3B) — `rebuildPhaseSnapshot(phase): GamePhase`: the snapshot **dispatcher**. Reads authoritative state through `GameState`/`PlayerSessionStore`, never mutates it, and delegates every formula to a projection module. Snapshot-bearing phases: `world_map`, `equip_screen`, `debug_equip_screen`, `camp`, `upgrade_tree`, `battle_results`, `battle`. Snapshotless phases (`main_menu`, `map_victory`, `debug_level_select`) are returned **by identity**, not copied. The switch ends in a `never` assertion instead of a permissive `default: return phase`, so a new `GamePhase` variant is a compile error until someone makes an explicit snapshot decision. The `battle_results` case deliberately never touches battle runtime — it is already cleared by the time this runs. Registered as a `snapshot` facade in `ORCHESTRATION_COLLABORATOR_REGISTRY`: its three state readers and six projections are each an explicit reviewed edge, and no lifecycle owner or effects module can be added without changing the facade's kind
- `battlePhaseSnapshot.ts` (Stage 3B) — `buildBattlePhaseSnapshot(phase, runtime)`: the one battle-phase render/control projection (unit views, occupancy, field cells, active unit, mode/side, manual control flags, charge state, target highlight, preview target, combat-entry validity, participants). The caller owns runtime resolution and `sessionSource` validation, so this function receives an already-validated runtime. The active unit is looked up through `fieldUnits`, never `unitsById` — the round queue is field-only by invariant and `activeUnit` is typed `FieldBattleUnitSnapshot`. The target-policy switch is a returning helper whose `never` branch is a genuine compile-time exhaustiveness check. No `GameState`, `PlayerSessionStore`, Phaser, scene or mutation import
- `phaseChangeNotifier.ts` (Stage 3B) — `notifyPhaseChanged()`: owns exactly `EventBus.emit(Events.STATE_CHANGED)`. Deliberately separate from `PhaseSceneSynchronizer`: navigation starts and stops scenes, a mutation-only transition just asks the already-running scene to re-read `getPhase()`. It never syncs scenes, reads or mutates state, inspects phases or actions, or imports Phaser. Banned from scenes by `SCENE_PIPELINE_BANNED_IMPORTS` — a scene that could emit it would be able to fake a phase change
- `phases.ts` — `GamePhase` and `PhaseAction` discriminated unions; the contracts between UI and core
- `GameState.ts` — runtime singleton owning **campaign and debug containers only**: `CampaignState` and `DebugBattleState`. Battle runtime left in Stage 4B — it lives in `battleRuntimeStorage` behind the two gateways above, so no module gains the ability to replace a battle attempt merely by importing `GameState`. Both of its surfaces are **closed policies**, not incidental properties of the current file: `GAME_STATE_FIELDS` pins the two stored fields and `GAME_STATE_PUBLIC_API` pins the eleven public methods, checked against the real source by `scripts/state-ownership-policy.mjs`. Both dimensions are needed — a field registry alone would not see a capability re-exposed as a public accessor over module-local storage, and the policy additionally rejects index signatures, computed/unnamed members, ECMA-private members, unregistered constructor parameter properties, and a registered state field that has become public. Exposes only replacement methods (`setCampaignState`, `replaceCampaignRoster`, `replaceCampaignInventory`, `setDebugState`, `replaceDebugRoster`, `replaceDebugInventory`, ...) — no writable flat fields. `getDebugState(): DebugBattleState | null` is for genuinely optional reads; `requireDebugState(): DebugBattleState` throws `'Debug state is not initialized'` and is mandatory wherever the current phase or runtime explicitly identifies the session as `debug` — an explicit `PlayerSessionSource` selects exactly one storage tree, and missing storage for that explicit source is a lifecycle error, never a silent fallback to the other tree. Importer-restricted (`STATE_STORE_RESTRICTED_TARGETS`) and non-forwardable — no re-export, alias or dynamic `import()`, enforced by `scripts/state-store-forwarding-policy.mjs`. No pinned export surface: it exports a `const`.
- `battleExit.ts` (Stage 8) — `applyBattleResult({ runtime, session, outcome }, playerBlueprints = PLAYER_UNITS): RosterState`: the single source-neutral battle-result pipeline. Placement (`applyFieldPlacementsToRoster`) → runtime HP/life (`applyBattleExitPlayerPersistence`) → victory level-up (`applyVictoryLevelUpPersistence`), all composed into one roster before the caller writes anything. Its runtime parameter is `Pick<BattleRuntimeContext, 'state' | 'participants'>` — `sessionSource`, mode, replay setup, turn context and pending intentions are out of scope **by type**, so the rules cannot branch on the source even by accident. Level-up inputs are read from the post-battle roster, never from `session.roster`: the pre-battle roster would silently revert damage, death and revive. The blueprint parameter mirrors `projectPlayerBattleSetup()` so tests inject minimal definitions.
- `battleResultsSnapshot.ts` (Stage 8) — `buildBattleResultsSnapshot(roster, seeds)`: the result-screen read model. Level and life state come only from the final stored roster of the session named by `battle_results.sessionSource`; the seeds carry immutable presentation metadata (`templateId`, `name`, `wasOnBench`, `spriteKey`) that no longer exists once the runtime is cleared. A missing roster record is a thrown lifecycle error.
- `battleRuntimeContext.ts` (Stage 4) — `BattleRuntimeContext`: the single owner of all mutable data for one active battle attempt (`state`, `participants`, `replaySetup`, `turnContext`, `mode`, `sessionSource`, `pendingAutoTurnIntention`). A battle phase is active if and only if the `battleRuntimeStorage` slot is non-null; `runtime.sessionSource` must always equal the active battle phase's `sessionSource` — validated by `requireBattleRuntimeForPhase()` (`battleRuntimeAccess.ts`) at every read/mutation site (snapshot rebuild, lifecycle/control/preview/turn/placement side effects, replay, exit), not only at render time. `createEmptyBattleState()` and `createBattleRuntimeContext()` are the only ways to produce a `BattleState` or a runtime; each returns independent, non-aliased collections. There is no separate restart factory — a replay builds one complete replacement runtime through the same `createBattleRuntimeContext()`. `BattleRuntimeContext` is never serialized and never exposed to scenes — `GamePhase.participants` is always a copy of `runtime.participants`, never the same array reference, so render data and persistence input never alias runtime-owned mutable state. `BattleReplaySetup` holds only the captured enemy placements needed to reconstruct the formation; the encounter ID lives on the battle phase and is not duplicated here. It is not a full deterministic battle snapshot.
- `playerSessionState.ts` — `PlayerSessionSource = 'campaign' | 'debug'`, a core-only routing key (must never be imported by `inventory/`, `progression/`, `battle/`, `campaign/`, `world/`); `PlayerSessionState { roster, inventory }`, the core-only composition of the two player-owned domains; used by `DebugBattleState.session`
- `playerSessionStore.ts` — `PlayerSessionStore`: the sole seam that resolves a `PlayerSessionSource` to campaign or debug runtime storage (`getSession`, `replaceRoster`, `replaceInventory`, `replaceSession`). `replaceSession` exists for operations that change **both** player domains at once (consumable use): two sequential `replaceRoster` + `replaceInventory` calls would publish an intermediate state in which the bonus is granted but the item still exists. It preserves everything outside the session — campaign world and money, debug `initialConfig`. Storage-only — contains no inventory/progression/validation rules. Its complete dependency set is an exact Stage 4C policy in `ORCHESTRATION_COLLABORATOR_IMPORT_POLICIES`: the two player-owned domain contracts and the state singleton, and no domain rule. Importer-restricted and non-forwardable like `GameState`, on its own importer list — approval for one store grants nothing for the other.
- `equipmentScreenSnapshot.ts` — `buildEquipmentScreenPlayerSnapshot(session, selectedUnitTemplateId, interaction)`: the one player-equipment projection shared by both `equip_screen` and `debug_equip_screen`. Takes a `PlayerSessionState` directly — never reads `GameState`, never branches on campaign/debug. It also projects `itemUsage` (per backpack `usable`/`consumable`, via `itemUsability`), `itemActionMenu` and `pendingItemUsePrompt` — the latter two being the two open states of ONE storage cell, so both can never be non-null. The window's `equip` option is decided by `inventory.evaluateEquipItem`, the full precondition set of `equipItem`, never the narrower `canUnitEquipItem`; nothing here calls a mutation function to learn an answer. Its import allowlist admits `core/itemUsability` and **not** `core/itemUse`, so a read-side module has no path to a roster/inventory replacement. Omitting an unjustifiable prompt or window here HIDES it; DISPOSING the interaction is the item-use phase handler's job — different operations, different owners, different modules.
- `phaseHandlers/inventoryPhaseHandler.ts` — `applyEquipmentPhaseAction({ source, action })`: the only mutation point for `equip_item`/`unequip_item`. Resolves the session via `PlayerSessionStore`, delegates to the pure `inventory/` domain, writes back only on success. No Phaser, no phase-type/backpack-ID knowledge.
- `phaseHandlers/campPhaseHandler.ts` — `applyCampPhaseAction({ source, action })`: the only mutation point for `toggle_camp_unit`. Resolves the session via `PlayerSessionStore`, delegates to `progression/rosterCamp.ts`'s `toggleUnitCampStatus()`, writes back only on success. Contains no camp rules itself.
- `phaseHandlers/progressionPhaseHandler.ts` — `applyChooseUpgradePhaseAction({ source, unitTemplateId, action })`: the only mutation point for `choose_upgrade`. Resolves the session via `PlayerSessionStore`, delegates to `progression/rosterUpgrades.ts`'s `chooseUnitUpgrade()`, writes back only on success. Contains no upgrade rules itself.
- `rosterCampSnapshot.ts` — `buildRosterCampSnapshot(roster)`: the one camp read-model projection shared by `camp` and `debug_equip_screen`. Takes a `RosterState` directly; builds the `PLAYER_UNITS`-ordered `CampUnitSnapshot[]` display list and forwards `selectedForBattleUnitCount`/`activeLivingUnitCount`/`canStartBattle` from `progression/rosterCamp.ts`'s `getRosterPartyStatus()` — does not recompute any of them.
- `upgradeTreeSnapshot.ts` — `buildUpgradeTreePlayerSnapshot(roster, templateId)`: the one upgrade-tree read-model projection shared by campaign and debug. Takes a `RosterState` directly; returns the empty placeholder if the unit or blueprint is missing.
- `initCampaignState.ts` — pure campaign factory; the only place `CampaignState` is constructed
- `debugPlayerSession.ts` — pure debug session factory; the only place a debug `PlayerSessionState` is constructed. Must remain a pure function of `(config, playerUnits, itemCatalog)` — no RNG, no clock, no hidden `GameState` reads — because that purity is what makes `resetDebugSession()` a reproducible operation
- `debugLifecycle.ts` (Stage 9) — the single owner of debug-session _container_ lifecycle: `initializeDebugSession(config)`, `resetDebugSession()`, `clearDebugSession()`. This is a different seam from the `phaseHandlers/*` files below: those mutate the _contents_ of an already-existing session, resolved via `PlayerSessionStore.getSession(source)`; `debugLifecycle.ts` creates, replaces, or destroys the _container_ (`DebugBattleState` itself) and writes through `GameState.setDebugState()`/`clearDebugState()` directly. It contains no roster/inventory/progression/battle-result rules. Its complete dependency set is an exact Stage 4C policy in `ORCHESTRATION_COLLABORATOR_IMPORT_POLICIES`, which deliberately excludes campaign construction so the two lifecycles stay separable
- `DebugBattleState.ts` — `{ session: PlayerSessionState, initialConfig: DebugSessionConfig }`; owned by `GameState`, never placed inside `CampaignState`. `initialConfig` is what `resetDebugSession()` rebuilds from
- `worldMapProjection.ts` — the **read-only** world projection: `projectWorldMapSnapshot` (CampaignState.world + roster party status → phase snapshot). Pure, and deep-clones `entityStates`/`partyPos` so no scene holds a live reference into campaign state. Party status rides on the world-map snapshot so `resolveTransition` can reject `enter_battle` for an invalid party without reading roster state. It no longer exports `applyMovePartyToCampaign` — campaign-world writes moved to `campaignWorldTransitions.ts` in Stage 3C, so a read-side module can never be the route to a mutation. Stage 4C makes that enforceable rather than conventional: its exact policy in `ORCHESTRATION_COLLABORATOR_IMPORT_POLICIES` admits no store and no transformation module, in both directions.
- `battleActionFeedback.ts` (Stage 3A) — the **public** transient battle-feedback contract: `BattleActionFeedback` (`events`, `directive?`, `winner?`, `autoTurnDirective?`, `autoTurnApplied?`) plus its narrowed DTOs `BattleTurnDirectiveFeedback`, `BattleAutoTurnDirectiveFeedback` and `AutoTurnIntentionFeedback`. Every nested type is declared here rather than re-exported from a runtime or handler module, and the module imports only `shared/gridTypes` + `battle/battleEvents` — deliberately _not_ `battle/turnResolver`, so no internal directive type can leak in by widening. This is presentation/control output belonging to one `transition()` invocation: it is never render state (that is `GamePhase`), never retained across transitions, and shares no object reference with `BattleState`, `TurnContext` or `BattleRuntimeContext` at any depth.
- `phaseTransitionResult.ts` (Stage 3A) — the **public** return contract of `PhaseManager.transition()`: `{ status: 'rejected' }` or `{ status: 'applied', battleFeedback }`. Carries no `GamePhase` (render state comes from `getPhase()`) and no navigation/mutation classification — `transition()` computes that classification as a local variable only, because exposing it would invite scenes to make routing decisions that belong to `resolveTransition`.
- `phaseEffectsResult.ts` (Stage 3A) — the **internal** pipeline value returned by `phaseActionEffects.apply()`: `{ battleFeedback }` and the shared frozen `NO_PHASE_EFFECTS`. Call-local by construction — never stored on the coordinator, never exposed to scenes (banned by `SCENE_PIPELINE_BANNED_IMPORTS`). Only the battle-turn branch returns feedback; every other branch returns `NO_PHASE_EFFECTS`. There is no `lastBattleTransition` field and no `getLastBattleTransition()` accessor: the stored-result protocol was removed so a later rejected or non-battle action has no way to observe an earlier battle action's feedback.
- `phaseTransitionResolver.ts` (Stage 2 of the current `PhaseManager` decomposition) — owns pure phase routing: `resolveTransition(currentPhase, action, metadata: PhaseTransitionMetadata)`, extracted out of `PhaseManager.ts` so it can be Node/vitest-tested without Phaser. Takes derived facts (currently just `{ mapCleared: boolean }`) as an explicit `metadata` parameter instead of reading `GameState` — `phaseTransitionMetadata.ts` computes that value (delegating the pure math to `../world/mapCompletion.ts`) and `PhaseManager` passes it in. **`PhaseTransitionMetadata` is declared in neither this file nor `phaseTransitionMetadata.ts`** (Stage 4A): it lives in the dependency-free `phaseTransitionMetadataContract.ts` and is **not** re-exported here, because a re-export would restore the bypass that split removes. Importable in Node with zero browser globals (`tests/core/phaseManagerNodeImport.test.ts`); enforced by its own exact allowlist (`PHASE_TRANSITION_RESOLVER_IMPORT_POLICY` in `scripts/orchestration-boundary-rules.mjs`, pinned by `tests/scripts/boundaryPolicy.test.ts`) — **not** by a `PURE_CORE_FILES` entry, which this file has never had. That allowlist is exactly `core/phases` + `core/phaseTransitionMetadataContract`, so `GameState`, Phaser, a store or a handler is rejected without being enumerated.
- `phaseTransitionMetadataContract.ts` (Stage 4A) — the neutral data contract between metadata derivation, the resolver and the coordinator: `PhaseTransitionMetadata` and nothing else. It exists because boundaries are enforced per module and a module-level allowlist cannot distinguish "may name the type" from "may call `resolveTransition`": declaring the type in the resolver would have given `phaseTransitionMetadata` routing authority, and declaring it in the derivation module would have forced the pure resolver to import a `GameState` reader. Its own import allowlist is **empty** and pinned — one import here would make it a shared dependency of both sides and re-open the edge. Banned from scenes for symmetry with `phaseEffectsResult` (an internal pipeline contract with no scene-facing purpose), not because naming the type would give a scene routing authority.
- `battleInitialization.ts` — battle state factory; builds auto-placed or replay battle states. `buildNewBattleState()` places players first (before any enemy RNG is consumed) and returns the ordered `playerPlacements` records alongside the state. `buildReplayBattleState()` returns `{ state, playerPlacements }` for the same reason: the replay attempt's participants must be built from _its own_ placement records
- `battleSetupProjection.ts` — `projectPlayerBattleSetup(session)`: the one player battle-setup projection shared by campaign and debug. Takes a `PlayerSessionState` directly; never reads `GameState`, never branches on campaign/debug, never mutates the session. Projects **every** unit outside camp, alive or dead — a persistent-dead unit becomes a fully resolved candidate whose factory produces a canonical dead runtime unit. A blueprint with no roster record is skipped, never synthesized
- `battleParticipants.ts` — `buildInitialBattleParticipants(state, placements)`: the one participant builder for campaign and debug. The placement records are the initial-deployment truth (`wasOnBench` comes from the record, never from current `state.deployments`), and `isAlive` from the battle-domain `isAlive()` — so a unit that began the battle dead is present with `isAlive: false`. Reads only `BattleState` + records
- `battleStart.ts` — the one owner of battle-attempt construction for campaign and debug. `createBattleRuntimeForSession()` builds a new encounter (party status → setup projection → initialization with RNG → participants → runtime); `createReplayBattleRuntimeForSession()` rebuilds an attempt (party status → setup projection → enemy restoration from the captured `replaySetup` → participants → runtime). Neither takes a `BattleRuntimeContext`: replay receives only `session`, `replaySetup` and `sessionSource`, because those are the only facts that cross an attempt boundary — the previous attempt's participants, state, mode and turn context are out of scope by construction, not by convention. Both are pure composition: they return a runtime, never install one, never import `GameState`, and never accept map metadata. Neither branches on `sessionSource`; replay additionally consumes no RNG. Both throw on an invalid party (shared `assertPartyCanStartBattle`), because `resolveTransition` must already have rejected the action
- `unitStatsSnapshot.ts` — computes base and final stats for display
- `unitUpgradePresentation.ts` — generates upgrade text, stat lines, and skill descriptions for UI
- `EventBus.ts` — `STATE_CHANGED` event singleton. Phaser-independent (a small hand-rolled emitter, not `Phaser.Events.EventEmitter`); notification-only, never carries domain state, and never replaces `PhaseManager.getPhase()` as the render-data source. Scenes subscribe via `scenes/sceneEvents.ts` or call `on`/`off` directly. The pipeline never emits it directly — `phaseChangeNotifier.ts` is the only producer of `STATE_CHANGED` inside the transition pipeline
- `phaseSceneSynchronizer.ts` — `PhaseSceneSynchronizer` interface (`sync(phase: GamePhase): void`); the only contract through which `PhaseManager` requests scene start/stop. No Phaser, no scene keys — the Phaser implementation (`PhaserSceneSynchronizer`, phase→scene-key mapping) lives in `scenes/phaserSceneSynchronizer.ts`, injected into `PhaseManager` via `PhaseManager.init()`
- `Constants.ts` — grid layout, bench config, combat constants (visual colors removed; see `src/objects/battleVisualTheme.ts`)
- `playerUnitPersistence.ts` — the pure owner of persistent player-unit transformations (life state, HP, level, placement), source-neutral throughout. `applyFieldPlacementsToRoster(roster, state)` is the **single placement rule** for both confirmed combat and battle exit: field-deployed player units, **living and dead alike**, get a copied `lastPlacement`; bench and undeployed units keep theirs; a field player with no roster record throws. `applyBattleExitPlayerPersistence(units, exits)` applies runtime HP/life only (placement is not its concern) and `applyVictoryLevelUpPersistence(units, levelUps)` applies levels; both treat a missing roster record as a thrown lifecycle error rather than a skipped input. `computeBattleExitPlayerPersistence(runtime)` takes a **required** runtime snapshot — there is no "missing runtime" fallback and `wasOnBench` never decides persistent HP or life state.
- `playerBattleExitProjection.ts` — `buildPlayerExitInputs(participants, state)`: runtime → persistence projection, HP/life only. It enforces the participant/runtime one-to-one invariant (see Invariants) and throws on any deviation. Placement is not projected here.
- `phaseHandlers/battlePhaseHandler.ts` — battle lifecycle/turn/placement action routing (placement delegates to `../battle/placementState`), split into two explicitly separated layers. `applyBattleLifecycleAction({ state, action })` is the **pure battle-lifecycle rule**: it decides whether combat may begin and signals `persistPlayerPlacements`; it never touches storage and never branches on campaign/debug. `applyBattleLifecyclePhaseAction({ source, state, action })` is the **application seam**: it runs the pure rule and, when the signal is set, persists the pre-action placement into the session selected by `PlayerSessionStore`. It contains no placement rules of its own. `PhaseManager` calls only the application-level function. `computeBattleExitRoster({ runtime, outcome })` is the matching seam for battle **exit** — and it COMPOSES ONLY: read `runtime.sessionSource` → `PlayerSessionStore.getSession()` → `applyBattleResult()`, returning the next roster. It stops there because an exit now settles two domains at once and both must reach storage in one `replaceSession` call, sequenced by `battlePhaseEffects.finalizeBattleSessionOnExit`. It contains no roster rules and no campaign/debug conditional; a missing debug session throws through `PlayerSessionStore`. `AutoTurnIntention` is owned by `battleRuntimeContext.ts` and is **not** re-exported here — it is authoritative runtime data, and scenes receive the narrowed `AutoTurnIntentionFeedback` instead. `projectBattleActionFeedback(result)` is the single seam that narrows the handler's authoritative `BattlePhaseActionResult` to the public `BattleActionFeedback`: it copies the five public fields explicitly (never by spread), copies the `events` array, and rebuilds the nested `directive` and `intention` objects field-by-field via exhaustive switches. Both nested projections are load-bearing, not defensive style: the internal `TurnStartDirective.await_manual_target` carries `validTargets` — the _same array reference_ installed into `BattleState.validTargets` — plus an internal `activeSkill`, and `PhaseManager` stores the _same_ `AutoTurnIntention` object it returns into `runtime.pendingAutoTurnIntention`. Forwarding either would hand a scene a live handle into committed runtime state. `BattlePhaseActionResult` stays a standalone declaration (not `BattleActionFeedback & { state; context }`) because the internal and public `directive` types differ.

## Structural Role

`core` → game logic and state authority; all other layers read from it, none write to it directly

## Data Flow

`PhaseAction` dispatched from scene
↓
`PhaseManager.transition()` — derives metadata via `derivePhaseTransitionMetadata()`, then validates via `resolveTransition()` (pure, no side effects)
↓
`phaseActionEffects.apply()` — dispatches to a domain owner, which mutates `GameState` /
`DebugBattleState` or installs a battle runtime; then runs the generic battle-runtime finalizer
↓
`rebuildPhaseSnapshot()` — reads state, produces new `GamePhase` snapshot
↓
`PhaseSceneSynchronizer.sync(phase)` — starts/stops scenes (navigation only); `notifyPhaseChanged()` on mutation-only transitions
↓
Scenes re-render from new `GamePhase`
↓
`transition()` returns `PhaseTransitionResult` — after the phase is committed and
after sync/notify, never before

`PhaseManager` never calls Phaser directly. It holds a `PhaseSceneSynchronizer` (injected via
`init()`) and validates it is initialized **before** `phaseActionEffects.apply()` runs for any
navigating transition — not only at the final `sync()` call — so a missing `init()` call aborts
with zero state mutation rather than partially mutating `GameState`. Rejected and mutation-only
transitions never require a scene synchronizer.

## Dependencies

- depends on: `src/battle/` (unit factory, placement logic, auto-place), `src/inventory/` (equip/unequip, equipment bonuses, inventory snapshots), `src/data/` (unit/enemy blueprints, item definitions), `src/progression/` (unit progression and skill resolution)
- used by: `src/scenes/` (all scenes call `PhaseManager.transition()`), `src/objects/` (reads snapshots from `GamePhase`)

## Invariants

- `resolveTransition()` must remain pure — no Phaser calls, no state mutations
- All state mutations are reached exclusively through `phaseActionEffects.apply()`, which owns no
  mutation itself — it dispatches to a domain owner (`campaignLifecycle`, `debugLifecycle`,
  `battlePhaseEffects`, or a `phaseHandlers/*` seam). Resolver rejection is silent and public
  (`{ status: 'rejected' }`, effects never called); an action the resolver **accepted** arriving
  with an inconsistent phase is internal lifecycle corruption and throws
- `DebugBattleState` must never be placed inside `CampaignState` and must never be serialized —
  it is stored beside campaign state in `GameState`, not inside it
- Campaign and debug state must never share mutable `RosterState`/`InventoryState` instances,
  even though they use the same types
- Equipment, camp, and upgrade source (`PlayerSessionSource`) must be read once from the current
  phase's `sessionSource` field (`equip_screen`/`debug_equip_screen`/`camp`/`upgrade_tree`) —
  never derived from `phase.type`, `returnPhase`, debug-state presence, or backpack ID
- `PlayerSessionStore` is the only equipment/camp/upgrade path to campaign/debug storage;
  `equipItem`/`unequipItem`/`buildBackpackSnapshot`/`buildEquipmentSnapshot` operate on
  `InventoryState` and resolve the shared backpack structurally (`requireSharedBackpack`) — no
  equipment rule may compare `backpack_shared`/`backpack_debug` string IDs
- **Out-of-combat item use is split across domains and no layer absorbs another's rule.** `inventory/itemUseOps.ts` resolves and destroys the instance; `progression/consumableStatBoost.ts` applies the permanent bonus; `progression/itemHealing.ts` owns the current-HP restoration formula and the full-HP refusal; `core/itemUsability.ts` + `core/itemUse.ts` compose them. `progression/**` gains no dependency on `inventory/**` — composing two domains is the orchestration layer's job. The alive-HP clamp stays core-owned (`playerUnitPersistence.clampAliveCurrentHp`) and is resolved to a number before progression is called, so progression never imports core and nothing is duplicated. `permanent_stat_boost` and `heal` are executable; `heal` is executable for a `usable` as well as a `consumable`, so the SAME potion is drinkable from the backpack and activatable from `usable_slot` in battle. `revive` remains unimplemented and is rejected with `unsupported_effect`. The full-HP refusal (`unit_full_hp`) is heal-specific — a full-health character may still consume a permanent-stat essence

- **In-battle item activation keeps read and write responsibilities separate.**
  `battle/itemUsability.ts` owns effect support and eligibility and is available to
  `battlePhaseSnapshot`; `battle/itemUse.ts` owns execution and is available only to
  `phaseHandlers/battlePhaseHandler`. The battle domain receives narrow item resources and never
  accesses inventory or progression state directly.

- **Attempt item resources live on `BattleRuntimeContext`, never in `BattleState`.** `usableResources` (keyed by battle unit id) and `consumedItems` are attempt-scoped data that outlives every state transform and feeds exit settlement — the role `participants` already plays. `BattleState`/`Unit` are rebuilt by every transform, so a field there would have to be threaded through placement, death and revive for a value no battle rule reads; keeping it on the runtime is also what makes an unused potion stay with its owner while a used one does not return on revive, with no line in those transforms. Battle domain functions receive a NARROW `BattleItemResource` (`{ instanceId, name, effect }`) — never the runtime record, never a session. Effects are DEEPLY readonly (`ReadonlyItemUseEffect`) and copied at every boundary: a plain `readonly effect` field would still allow `effect.amount = 0`, and copying only the Map or the array would leave the authored data reachable
- **The item interaction is transient presentation state with a real owner.** It lives in the `itemInteractionStorage` triad, keyed by `PlayerSessionSource`, so a campaign transition can never read, select, confirm or dispose a debug interaction. One cell holds `choosing_action` OR `confirming_use`, which is why at most one modal can be open. `phaseActionEffects` holds none of it and writes none of it: it dispatches to `phaseHandlers/itemUsePhaseHandler`, which is the only importer of the write capability. Disposal happens on confirm, cancel, window close, an unhonourable selection, character change, equipment-screen exit, and session creation/reset/disposal — `reset_debug_session` needs its own explicit clear, because it keeps the same phase AND the same selected character, so the structural teardown never fires, and it recreates the same authored instance ids, so matching ids would not distinguish the stale request either
- Camp and upgrade mutation rules live only in `progression/rosterCamp.ts` and
  `progression/rosterUpgrades.ts`; `PhaseManager` and its phase handlers contain no camp or
  upgrade business rules — they only resolve a session and delegate
- **Battle runtime ownership has exactly one owner: the nullable `BattleRuntimeContext` in
  `battleRuntimeStorage`.** No battle state, mode, turn context, participants, replay setup, or
  pending auto-turn intention exists outside it, and it is no longer in `GameState`.
  `PhaseManager` never keeps its own `pendingAutoTurnIntention` field. Every battle-runtime
  change installs one complete replacement runtime via `installBattleRuntime()` — never two
  sequential writes for one action, and never a field-specific write: the partial setters
  (`replaceBattleState`/`Mode`/`TurnContext`/`PendingAutoTurnIntention`) were deleted in Stage 4B
  rather than rehomed. Reads go through `requireBattleRuntimeForPhase()`, which validates
  initialization and `sessionSource`; writes go through `battleRuntimeWriteAccess`, which
  `battlePhaseEffects` alone may import. Both directions are enforced by boundary policy, and
  the inbound registry compiles **fail-closed**: a malformed entry yields no enforceable policy
  at all rather than a partially active one that reads as satisfied
- Battle session (`sessionSource: 'campaign' | 'debug'`) is read once from the active `battle`
  `GamePhase`, exactly like `equip_screen`/`camp`/`upgrade_tree` — never derived from
  `returnPhase`, debug-state presence, map metadata, or action type. A missing `DebugBattleState`
  when `sessionSource === 'debug'` is a thrown lifecycle error, never a silent fallback to
  campaign storage
- Leaving the `battle` phase for any non-battle phase always clears the whole
  `BattleRuntimeContext` (`PhaseManager.transition()`, after side effects, before snapshot
  rebuild) — no completed or abandoned battle runtime survives into `battle_results` or any
  later phase
- **Battle selection and living-party validity are two different concepts, owned solely by
  `progression/rosterCamp.ts`.**
  `isSelectedForBattle()` (`isInCamp === false`) decides inclusion in battle setup and counts
  toward `MAX_SELECTED_BATTLE_PARTY_SIZE` — **dead units outside camp are selected**, because they
  receive a field or bench deployment like any other unit.
  `isActiveLivingUnit()` (`alive && !isInCamp`) governs `MIN_LIVING_BATTLE_PARTY_SIZE` and the
  last-living camp protection.
  `getRosterPartyStatus()` returns both counts plus `canStartBattle`. `rosterCampSnapshot.ts` and
  `worldMapProjection.ts` forward all three into `GamePhase` without recomputing them; scenes
  (`Prep.ts`, `UnitSelectionPanel.ts`, `WorldMap.ts`) consume `canStartBattle` for control state and
  the counts only for label text — no scene interprets the numeric bounds itself
- **Battle start has exactly one path:** `enter_battle` and `start_battle` share a single
  `battlePhaseEffects.startBattleRuntime()` that reads `sessionSource`/`enemyGroupId` from the already-resolved
  battle phase, resolves the session through `PlayerSessionStore`, and installs the result of
  `createBattleRuntimeForSession()` once. There are no campaign-specific or debug-specific setup
  adapters and no divergent participant construction
- **Party validity is enforced in `resolveTransition()`**, before any side effect runs, by reading
  `canStartBattle` off the current `world_map` / `debug_equip_screen` phase. An invalid party can
  therefore never partially mutate campaign or battle state, and no rejected-transition protocol is
  needed. Reaching `createBattleRuntimeForSession()` with an invalid party is a lifecycle error
- **`GamePhase.canBeginCombat`** is computed in `battlePhaseSnapshot.ts` from the battle-domain
  `canBeginCombat()` and enforced independently in `applyBattleLifecycleAction`. Scenes disable the
  begin-combat control from the boolean; they never inspect field membership or life state
- **Confirmed-placement persistence is source-neutral.** `battle_begin_combat` runs the same pure
  lifecycle rule and the same roster transformation for campaign and debug; only
  `applyBattleLifecyclePhaseAction` knows the `PlayerSessionSource`, and it uses it solely to select
  the storage tree. The **pre-action** `BattleState` is persisted, before the combat runtime is
  installed, so a lifecycle error aborts the transition with neither roster nor runtime written
- **Battle-exit results are source-neutral (Stage 8).** `exit_battle` runs one pure pipeline
  (`battleExit.ts`) over the runtime and the selected `PlayerSessionState`, and writes the
  result through exactly one `PlayerSessionStore.replaceSession()` call in
  `finalizeBattleSessionOnExit`. Equivalent campaign and debug sessions receive equivalent
  roster changes for the same runtime and outcome: actual runtime HP and life state are
  persisted, field deployments overwrite `lastPlacement` while bench participants keep theirs,
  victory levels **every participant exactly once** — field, bench, dead and revived alike —
  and defeat grants no levels. Nonparticipants, including camp units, are returned by identity.
  Debug injury and death therefore persist across debug battles; `reset_debug_session`
  (dispatched from `debug_equip_screen`, restoring the session from its own `initialConfig`)
  is the direct recovery route when a debug session runs out of living units — see the
  debug lifecycle invariant below. `debug_equip_screen` → `return_to_debug_level_select` →
  `init_debug` remains available for starting a _differently configured_ session. The
  **only** `sessionSource`-keyed branch left in teardown is the
  campaign world consequence (marking the defeated encounter). `mapCleared`
  and `map_victory` remain metadata-driven, not source-driven — the map-clear check itself
  (`wouldMapBeClearedAfterDefeatingMob()`) lives in `src/world/mapCompletion.ts`, and the
  stateful lookup around it lives in `core/phaseTransitionMetadata.ts` — neither is in
  `PhaseManager.ts` any more (Stages 2 and 3B of the `PhaseManager` decomposition)
- **Battle participants and runtime player units are one-to-one at exit.** Player participants
  are persistent roster entities: death changes life state and never removes the unit from
  `BattleState.units`, and player-side battle-only summons are not supported. Every deviation —
  a duplicate participant or runtime `templateId`, a participant with no runtime unit, a runtime
  player unit with no participant, a participant with no deployment — is thrown as lifecycle
  corruption by `buildPlayerExitInputs()`. Adding summons later will require an explicit
  persistent-vs-battle-only entity classification at this seam
- **`battle_results` carries no dynamic battle data.** `participantSeeds` holds immutable
  presentation metadata only (`templateId`, `name`, `wasOnBench`, `spriteKey`); final level and
  life state are rebuilt by `rebuildPhaseSnapshot()` from the roster selected by
  `battle_results.sessionSource`. A battle-start `level`/`isAlive` snapshot can therefore not
  reach the result screen — it is excluded by type, not by convention
- **Replay has exactly one policy.** `replay` runs the same pipeline for campaign and debug:
  it restores the captured enemy templates, levels and anchors from `runtime.replaySetup`, and
  re-projects player units from the **current** `PlayerSessionState`. Participants are rebuilt
  from the replay attempt's own `PlayerInitialPlacement[]` — a previous attempt's participant
  snapshot is never carried forward, because Stage 6 placement persistence can change the
  field/bench split between attempts and `wasOnBench` feeds exit persistence. This is enforced
  by the factory's signature, not by discipline: only `replaySetup` and `sessionSource` cross an
  attempt boundary, so the previous runtime's participants, state, mode and turn context are not
  in scope inside the replay pipeline. Player placement therefore comes from persistent
  `lastPlacement`, which after Stage 6 normally represents the confirmed pre-combat layout of
  the previous attempt. Replay is not a deterministic runtime or RNG snapshot. **Debug replay no
  longer rerolls enemies** — starting a new debug battle is the operation that creates a new
  encounter. `battlePhaseEffects.replayBattleRuntime()` contains no conditional: it resolves the
  session by `runtime.sessionSource` and installs the returned runtime once. Replay is
  `battle → battle` navigation, so the generic teardown never fires for it
- **Debug session lifecycle has exactly one owner: `debugLifecycle.ts`.** `phaseActionEffects`
  only decides which lifecycle operation applies (`init_debug`, `reset_debug_session`,
  `exit_to_menu`, `new_game`) and owns the RNG/battle-runtime reset sequence around it —
  it never reconstructs roster or inventory records itself. Every lifecycle action replaces
  the whole gameplay RNG pair by calling the stream factory again (never reassigning an
  individual stream field) and defensively clears any stale `BattleRuntimeContext` via
  `clearBattleRuntimeIfPresent()`, because `exit_to_menu`/`new_game` are the only two actions
  the resolver accepts from **any** phase and must dispose state even when dispatched from a
  non-battle phase (e.g. a damaged `debug_equip_screen`), which the generic "leaving `battle`"
  teardown does not cover. Dispatched _from_ battle both clears run — the explicit one and the
  finalizer — which is pre-existing, harmless, and deliberately not special-cased.
  `reset_debug_session` is accepted only from `debug_equip_screen` and is mutation-only
  (`resolveTransition` returns the same phase) — rejected from `battle`/`battle_results`
  so an in-progress attempt can never lose its owning session mid-flight.
- **Battle exit ordering is load-bearing, and session finalization comes FIRST.**
  `battlePhaseEffects.finalizeBattleSessionOnExit` runs in `phaseActionEffects.apply` **before**
  the action-specific dispatch, and is structural — "the previous phase is `battle` and the
  resolved one is not" — never an action catalogue. The ordering is forced, not stylistic:
  `exit_to_menu` clears the debug session and the battle runtime inside its own dispatch case and
  `new_game` reinitializes the campaign there, so settling afterwards would either write to a
  session that no longer exists or apply an outgoing attempt's records to a freshly created one
  that reuses the same authored instance ids. It computes the COMPLETE next session — the roster
  result (`computeBattleExitRoster`, compose-only) plus the settled inventory
  (`battleItemSettlement`) — and writes it with ONE `replaceSession`, because two sequential
  writes would publish a state in which the potion is gone but the battle damage is not yet
  recorded. `outcome === null` (abandonment, menu exit) settles the inventory and leaves the
  roster alone. Then `exit_battle` dispatch runs the campaign world consequence
  (`applyBattleWorldConsequence`) while the runtime is still installed, and generic teardown
  disposes it last. Each step resolves and validates the runtime inside its own body rather than
  trusting another to have done it

- **In-battle consumption is attempt-local until exit.** A drunk potion disappears from
  `usableResources` and from the action bar immediately, and one record is appended to
  `consumedItems` — inside the SAME `installBattleRuntime` call as the new state, so no runtime
  exists in which the HP is restored but the item is still available. Equipment and backpack
  mutation stay inaccessible during combat, and the persistent removal happens exactly once, at
  exit. `replay` is `battle → battle`, so finalization never fires for it: the discarded
  attempt's records are simply dropped and the fresh runtime is rebuilt from the current session,
  which is why a replay restores the potion — nothing is rolled back. An out-of-combat use is
  already permanent in the session and is therefore NOT restored by a replay
- **Generic battle-runtime teardown is structural, not an action catalogue.**
  `teardownBattleRuntimeAfterTransition(previousPhase, resolvedPhase)` clears the runtime when
  the previous phase is `battle` and the resolved phase is not — comparing phase _types_, never
  object identity (replay resolves to a copy of the battle phase) and never action names (which
  would silently miss future battle-to-non-battle transitions)
- Scenes never call `this.scene.start/stop` — only `PhaseManager` does
- `GamePhase` is the single source of truth for every scene's render data; `WorldMap` reads
  its map state and party position from the `world_map` phase snapshot, never from `GameState`
  directly

## Battle Phase Boundary

Battle lifecycle routing is owned by `phaseTransitionResolver.ts`, runtime installation/mutation/disposal by `battlePhaseEffects.ts` (dispatched from `phaseActionEffects.ts`), and snapshot rebuilding by `phaseSnapshotRebuilder.ts`. `PhaseManager` sequences these and owns none of them.

`rebuildPhaseSnapshot()` (`phaseSnapshotRebuilder.ts`) and `buildBattlePhaseSnapshot()` (`battlePhaseSnapshot.ts`) are the bridge from mutable `GameState` / `BattleState` to the scene-facing render/control snapshot. The battle projection may import battle formulas (`getActiveSkill`, `compileSkillUsePlan`, `canBeginCombat`, `hasChargedThisRound`) to compute snapshot fields — this is correct because core is deriving presentation-relevant data from authoritative state.

**Snapshot value isolation.** No snapshot field aliases runtime-owned mutable state. `participants`, `roundQueue`, `validTargets` (and each coord), `placementSelection`, and every unit's `shape`, `deployment`, `sprite.states`, `skills` array and `activeEffects` (including nested `effect` / `periodicHp`) are copied by `battlePhaseSnapshot.ts` / `battleSnapshotBuilder.ts`. Two of these are load-bearing rather than defensive style: `unit.shape` aliases an entry in the shared `SHAPES` registry (`data/shapeDefinitions.ts`), so every unit of the same shape holds the _same_ object and one scene-side mutation would corrupt that shape process-wide; `activeEffects` is runtime-owned at every depth. Both are now also `readonly` by type (see the read-gateway bullet in the root `CLAUDE.md`), but `readonly` is erased at runtime, so these copies remain the actual runtime guarantee — the two mechanisms are complementary and neither replaces the other. The single documented exception is the `ActionSkillDefinition` entries inside `skills` — shared, immutable static content from the `SKILLS` registry, exposed as `readonly` and deliberately **not** deep-cloned: they are not runtime state, and cloning whole skill trees on every rebuild would land on a hot path (`battle_preview_target` fires on pointer move).

**Reverse-covered dependencies (Stage 4C).** Every snapshot projection dispatched from `phaseSnapshotRebuilder` — `battlePhaseSnapshot`, `battleResultsSnapshot`, `equipmentScreenSnapshot`, `rosterCampSnapshot`, `upgradeTreeSnapshot`, `worldMapProjection` — carries an exact, reviewed import policy in `ORCHESTRATION_COLLABORATOR_IMPORT_POLICIES`, checked against the real source in both directions. None may acquire a state store, a lifecycle owner, a write gateway or `phaseActionEffects`; a removed dependency must be removed from the policy too, rather than left behind as a dormant permission. The obligation to carry a policy is derived from `ORCHESTRATION_COLLABORATOR_REGISTRY`, so a newly registered projection fails `check-boundaries.mjs` until someone reviews its dependencies. This constrains which modules a projection may reach — not what an approved function body does with them.

**Acceptable `GameState` reads** — the enforced list in `STATE_STORE_RESTRICTED_TARGETS`: `campaignLifecycle`, `debugLifecycle`, `phaseHandlers/worldPhaseHandler`, `phaseSnapshotRebuilder`, `phaseTransitionMetadata`, `playerSessionStore`. `PlayerSessionStore` has its own list: `battlePhaseEffects`, `phaseSnapshotRebuilder`, and the five session-mutating phase handlers (`battlePhaseHandler`, `campPhaseHandler`, `inventoryPhaseHandler`, `itemUsePhaseHandler`, `progressionPhaseHandler`) — `itemUsePhaseHandler` was added by an explicit importer-policy change, the mechanism the root `CLAUDE.md` names for a new direct consumer. `phaseActionEffects` is on neither — the facade sequences owners and never touches a store.

**Not acceptable:** direct `GameState` reads in scenes, scene controllers, or battle render paths. Use `PhaseManager.getPhase()` and snapshot fields instead.

## Where to Modify

- Add a new game screen → `phases.ts` (new `GamePhase` variant) + `phaseTransitionResolver.ts` (`resolveTransition`) + `phaseActionEffects.ts` (the dispatcher's `never` branch forces an explicit effects decision) + `scenes/phaserSceneSynchronizer.ts` (add the new phase's entry to `PHASE_SCENE_KEYS`; the mapping is a compile-time-exhaustive `Record<GamePhase['type'], GameplaySceneKey>`, so a missing entry is a build failure, not a silent no-op)
- Change a phase-routing rule (which transitions are valid, what the next phase looks like) → `phaseTransitionResolver.ts` (`resolveTransition`), not `PhaseManager.ts`
- Change how `{ mapCleared }` (or future transition metadata) is computed → `phaseTransitionMetadata.ts` for the stateful lookup, `src/world/mapCompletion.ts` for the pure map-completion rule
- Add a new player action → `phases.ts` (`PhaseAction`) + `phaseTransitionResolver.ts` (routing) + `phaseActionEffects.ts` (effects dispatch — the exhaustive switch will not compile until the new variant is handled). Never `PhaseManager.ts`: the coordinator is action-agnostic
- Change battle placement logic → `phaseHandlers/battlePhaseHandler.ts`
- Change battle turn-flow action handling → `phaseHandlers/battlePhaseHandler.ts` (the rule) and `battlePhaseEffects.ts` (`applyBattleRuntimeMutation`, the runtime write)
- Change what a confirmed placement writes back to the roster → `playerUnitPersistence.ts` (`applyFieldPlacementsToRoster`); change which session it is written to → `phaseHandlers/battlePhaseHandler.ts` (`applyBattleLifecyclePhaseAction`)
- Change what a battle result does to the roster (HP, life state, placement, level-up) → `battleExit.ts` (`applyBattleResult`); change how it is composed for an exit → `phaseHandlers/battlePhaseHandler.ts` (`computeBattleExitRoster`); change which session it is written to, or what else is written with it → `battlePhaseEffects.ts` (`finalizeBattleSessionOnExit`)
- Change what the post-battle result screen shows → `battleResultsSnapshot.ts`
- Change battle snapshot fields (what controllers see) → `battlePhaseSnapshot.ts`
- Change which phases carry a snapshot, or which state source one reads → `phaseSnapshotRebuilder.ts`
- Change the mutation-only refresh signal → `phaseChangeNotifier.ts`
- Change how units are initialized for battle → `battleInitialization.ts` / `battleSetupProjection.ts`
- Change how a battle attempt is started or replayed (campaign or debug) → `battleStart.ts`
- Change the battle-start participant snapshot → `battleParticipants.ts`
- Change upgrade stat/skill resolution → `src/progression/`
- Change unit snapshot views (bench/field/lookup), or what a unit snapshot copies vs shares → `battleSnapshotBuilder.ts` (`buildBattleUnitSnapshotViews`, the canonical battle phase read-model builder; see the snapshot value-isolation contract above)
- Change stat display computation → `unitStatsSnapshot.ts`
- Change upgrade description text → `unitUpgradePresentation.ts`
- Change grid/bench layout constants → `Constants.ts`
- Change debug session initial config/contracts → `DebugBattleState.ts`; change how a debug session is built → `debugPlayerSession.ts`; change debug session create/reset/dispose lifecycle → `debugLifecycle.ts`; change the production entry point/default config for a new debug session → `debugLifecycle.ts` (`initializeDebugSessionForLevel`); change when/how that lifecycle runs (RNG reset, runtime cleanup, which action triggers it) → `phaseActionEffects.ts`
- Change campaign initial content/config → `src/data/campaignInitialStateDefinition.ts`; change how a campaign is built → `initCampaignState.ts`
- Change the world_map snapshot projection → `worldMapProjection.ts`; change a campaign-world transformation (party movement, encounter defeat) → `campaignWorldTransitions.ts`; change which one runs when → `phaseHandlers/worldPhaseHandler.ts`
- Change how a new campaign is created → `campaignLifecycle.ts` (`initializeNewCampaign`); change what it contains → `initCampaignState.ts` + `data/campaignInitialStateDefinition.ts`
- Change battle runtime installation, replay, mutation routing or disposal → `battlePhaseEffects.ts`
- Change the effects dispatch table, a cross-domain lifecycle ordering, or RNG stream ownership → `phaseActionEffects.ts`
- Change cross-scene event wiring → `EventBus.ts` (Phaser-independent notification emitter) / `scenes/sceneEvents.ts` (Phaser scene-lifecycle binding)
- Change how `PhaseManager` synchronizes Phaser scenes → `scenes/phaserSceneSynchronizer.ts` (the contract it depends on is `phaseSceneSynchronizer.ts`, which stays Phaser-free)
- Change equip/unequip mutation logic → `phaseHandlers/inventoryPhaseHandler.ts`
- Change whether an item may be used out of combat → `itemUsability.ts`; change what a successful use does → `itemUse.ts` (plus `inventory/itemUseOps.ts`, `progression/consumableStatBoost.ts` and `progression/itemHealing.ts` for the domain halves); add a new healing potion → `data/items/usable/healing.ts` and nothing else
- Change whether an equipped item may be activated in battle, or which effects are supported → `battle/itemUsability.ts`; change what activation does → `battle/itemUse.ts`; change when the consumed item becomes permanent → `battleItemSettlement.ts` + `battlePhaseEffects.finalizeBattleSessionOnExit`
- Change the item-interaction lifecycle (when a window or confirmation is opened, disposed or committed) → `phaseHandlers/itemUsePhaseHandler.ts`; change how an interaction is stored or who may reach it → `itemInteractionStorage.ts` / the two gateways plus `RESTRICTED_IMPORT_TARGETS`
- Change what the equip screens render (backpack, equipment, stats, skills, sprite, unit tabs) → `equipmentScreenSnapshot.ts`
- Change how campaign vs. debug storage is resolved for equipment/camp/upgrade → `playerSessionStore.ts`
- Change camp mutation rules (last-active-unit invariant) → `src/progression/rosterCamp.ts`; change camp mutation routing → `phaseHandlers/campPhaseHandler.ts`
- Change upgrade selection validation rules → `src/progression/rosterUpgrades.ts`; change upgrade mutation routing → `phaseHandlers/progressionPhaseHandler.ts`
- Change what the camp screens render (roster list, active count, battle-entry validity) → `rosterCampSnapshot.ts`
- Change what the upgrade-tree screen renders (tiers, options, lock state) → `upgradeTreeSnapshot.ts`
